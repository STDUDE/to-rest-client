<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColor">@color/LittleTextColor</item>
        <item name="android:textColorHint">@color/HintTextColor</item>
    </style>

    <style name="PrimaryTheme" parent="AppTheme">

    </style>

    <style name="PrimaryTheme.Block" parent="PrimaryTheme">
        <item name="android:padding">5dp</item>
        <item name="android:background">@color/TouristBackground</item>
    </style>

    <style name="AppTheme.NoActionBar.Button" parent="Base.Widget.AppCompat.Button">
        <item name="android:background">@color/ButtonBackground</item>
        <item name="android:textColor">@color/MediumTextColor</item>
        <item name="android:padding">15dp</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
